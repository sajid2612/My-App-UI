import React, {
  Component
} from 'react';
import {
  Layout
} from 'antd';

const {
  Header,
  Footer,
  Sider,
  Content,
} = Layout;

export default class MainFooter extends Component {
  state = {}
  render() {
    return ( <
      Footer > Footer < /Footer>
    )
  }
}